class Enlace:
    def enquadramento(tipo, dado):
        framed_msg = ''
        if(tipo == "Contagem de caracteres"):
            print(tipo)
        elif(tipo == "FLAGS e inserção de bytes ou caracteres"):
            print(tipo)
        elif(tipo == "FLAGS Inserção de bits"):
            print(tipo)
        
        return framed_msg
